import {
  fromnowDate
} from 'ember-swacch/helpers/fromnow-date';

module('FromnowDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = fromnowDate(42);
  ok(result);
});
